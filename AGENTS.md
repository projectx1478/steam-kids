# AGENTS.md

全エージェント共通の正本。ツール固有の記述は`docs/agents/<tool>.md`に置く（索引は下記）。CLAUDE.mdは本ファイルを`@AGENTS.md`でimportし、Claude Code固有の記述のみ追加する。

## ツール別の追加ファイル

| ツール | 追加で読むファイル |
| --- | --- |
| Claude Code | `CLAUDE.md`（`@AGENTS.md`import込み・自動読込） |
| OpenCode | `docs/agents/opencode.md` |
| Kimi Code CLI | `docs/agents/kimi.md` |

## 起動時の必須手順

最初のツール呼び出しは`git fetch origin main && git log --oneline HEAD..origin/main`（セッション再開時に古い作業ツリーを最新と誤認しないため。出力があれば`git merge origin/main`で取り込む）。次に自分の`docs/agents/<tool>.md`があれば読み、続けて`PROJECT.md`→`SESSION.md`の順にReadする。読む前に編集・提案を始めない。矛盾時は本ファイル（Claude Code利用時はCLAUDE.md）＞PROJECT.md＞チャット指示の順。mainブランチ上にいる場合、編集前に「GitHub運用」に従い`origin/main`起点の作業ブランチへ切り替える。

チャット履歴は参考情報、仕様・設計・進捗はファイルが正。必要なファイルのみ読み込む（全体走査は不用意に行わない）。

## 開発方針

- 既存機能を壊さない差分修正が基本。不要なリファクタリング・範囲外の変更は行わない
- 既存の命名規則・コーディングスタイルを維持し、仕様不明時は推測せず確認する
- コード修正前に実装方針を提示し承認後に着手する

## 調査フェーズ（編集前に必須）

変更対象を1文字も書く前に、(a) 対象ファイルをRead、(b) 変更する関数・変数の呼び出し元をgrep、(c) 類似実装を1つ以上確認する。「たぶんこうなっている」で書かない。ファイルを読まずにimport・関数名・APIを書いてはならない。

## 計画フェーズ

変更するファイルと各ファイルで何を変えるかを箇条書きで提示してから着手する。3ファイル以上に及ぶ場合、または仕様が一意に定まらない場合はユーザーの承認を待つ。設計・調査と実装で使うモデル／エージェントを切り替えるツールは、切替タイミングの手順を`docs/agents/<tool>.md`に従う。

## 編集フェーズ（最小変更）

依頼された変更のみを行う。フォーマット統一・リネーム・未使用コード削除・コメント追加は、依頼に含まれる場合のみ。既存の命名規則とコーディングスタイルに合わせる。ファイル全文のwriteは新規作成時のみ。既存ファイルは必ず差分編集する。

## 役割分担（ティア）

- 設計・原因調査・conflict解消・PRマージ分岐・レビュー＝上位ティア（誤りの波及範囲が大きいため厚く配分）
- 実装＝実装ティア
- 確定済みテキストの非コード作業（複数ファイルへの同一コピー、typo・体裁修正）のみ軽量ティアを使ってよい。判断が1つでも入るなら軽量ティアは使わない
- クロスモデルレビュー推奨：実装したエージェントと別のモデルでPR差分をレビューする
- 具体的なモデル名の割当はSESSION.mdまたは各自のグローバル設定に置き、配布ファイルにハードコードしない（教訓：特定モデル名を設定ファイルへ書く設計は撤回済み）
- Claude Code固有のOpus/Sonnet/Haiku割当はCLAUDE.md参照

## バッチ実装

同時依頼の関連修正は1回の実装・1PRにまとめ（無関係な変更は混在させない）、コミットは論理単位で分けてよい。PR作成・マージ確認のツール呼び出し回数削減を優先する。

## 検証方針（AI代理実機確認）

- 設計案提示時は必ず「検証計画」を含め、確認項目はAI担当／ユーザー担当／省略（理由付き）に三分する
- ユーザー担当は人でなければ判定できない項目のみ（使い勝手、見た目、面白さ、体験の質）。動作・状態遷移・エラー処理・データ整合性・レイアウト崩れはAIが確認する
- AIはテストデータを自作し、外部APIは`page.route`で固定レスポンスに差替。手順は`.claude/verify/README.md`参照（検証時のみ）
- 判定はDOM（テキスト・属性・要素数・境界ボックス）で行う。スクリーンショットはユーザー確認用の保存物とし、AIが画像として読むのはDOM判定不能かつ1〜2枚で足りる場合のみ
- 省略基準：手動確認が1分以内の目視で済み、かつ自動化に大がかりな環境構築が必要な場合
- 完了時は既存シナリオも再実行し、「AI確認済み（結果）」「ユーザー確認依頼（手順・該当画像）」を報告

## 検証フェーズ（指示がなくても必ず実行）

編集後、PROJECT.md「検証コマンド」表に列挙されたコマンドを実行する。すべて成功するまで報告しない。失敗した場合は原因を特定して修正し、再実行する。実行していないコマンドの結果を書いてはならない。テストを通すためにテスト自体を書き換える・skipする・期待値をコードに合わせることを禁止する（テストの誤りだと判断した場合は修正せず報告する）。

## GitHub運用

- mainへの直接pushは禁止。ブランチ作成→コミット→PR作成、Mergeは人
- 例外：`sync-template-broadcast.yml`のテンプレート同期PR（project-templateと完全一致・保護ルール無しの場合のみ）はActionsが自動マージ。AIは実行しない
- 配布対象`.github/sync-files.txt`／配布先`.github/sync-targets.json`（配布先で編集しない）
- 既存ブランチへのpush前にPRのマージ・クローズ有無を確認。マージ済みなら`.claude/scripts/rebuild-branch.sh <branch>`で再構築。push後はオープンPRが無ければ新規作成
- ローカルmainは使わず信用しない。新規ブランチは`origin/main`起点、ローカルmain修正は`git fetch origin main && git branch -f main origin/main`でref上書き
- 作業再開・PRマージ後は`git fetch origin main && git log --oneline HEAD..origin/main`で確認し、未取り込みがあれば`git merge origin/main`後にファイルを読む
- 最終防衛線はGitHubブランチ保護（main直接push禁止・PR必須・force push禁止、Administration権限が必要なため設定は人が行う）。`.githooks/pre-push`（`git config core.hooksPath .githooks`で有効化。Codespacesは`.devcontainer/post-create.sh`が自動設定）とツール別deny設定（`.claude/settings.json`・`opencode.json`・`docs/agents/kimi.md`）はあくまで補助線

### 並行作業の衝突回避

- 1 Issue = 1ブランチ = 1エージェント。担当はIssueラベル`agent:claude`/`agent:kimi`/`agent:opencode`で宣言する（＝ロック）
- ブランチ接頭辞はツール別：`claude/*`・`kimi/*`・`opencode/*`
- 同一PCで同時稼働する場合は`git worktree add ../<repo>-<tool> -b <tool>/xxx origin/main`で作業ツリーを分離する（同じディレクトリで2エージェントを動かさない）
- SESSION.mdの「現在作業中のタスク」欄はIssue番号＋担当エージェントの1行表にする。タスク単位の引継ぎはIssueコメントに書く（SESSION.mdは並行更新でconflictするため、エージェント横断の恒久的な引継ぎのみを書く）

## Issue運用

バグ・改善点・仕様確認事項の発見時は直接修正せずIssue作成後に着手する（対応中タスクの軽微な修正は除く）。PROJECT.mdは恒久的な仕様のみ記載し、個別タスクはIssueへ。対応Issueはクローズ参照する。

## 機密情報のハードコーディング禁止

APIキー・DB認証情報・パスワード・トークン等の真の秘密情報は、クライアント側コード（Gitコミット対象）へ直書きせずサーバー側コンポーネントに隔離する。クライアントは認証トークン経由でのみアクセスする。採用技術・例外の扱いはPROJECT.mdの技術設計に従う。

## 終了条件

「次に〜します」で終わらない。宣言した作業はそのターン内で実行する。停止してよいのは、(a) 全検証が通った、(b) ユーザーの判断が必要な分岐に到達した、(c) 3回試して直らない、のいずれか。PR作成は停止点（(b)相当）。マージはユーザーが行う、AIは`gh pr merge`等を実行しない。

## 禁止事項

- 対象ファイル未読での編集／既存ファイルの全文write上書き
- 依頼範囲外の変更（自発的リファクタ・全体整形・未使用コード削除・コメント追加）
- 検証未実行の完了報告・結果記述
- `git reset --hard` / `git clean` / force push / `rm -rf`の無確認実行
- mainへの直接push、PRのマージ実行（`gh pr merge`・MCPの`merge_pull_request`等）。上記例外を除きMergeは人
- 秘密情報（APIキー・トークン・パスワード）の直書き、.env等の内容をチャットへ貼ること

## 報告形式

前置き・手順の実況・コード全文再掲を書かない。「変更内容（ファイル単位1行）」「実行した検証コマンドと結果」「次のアクションまたは要確認事項」の3点のみ。日本語で回答する。

## ドキュメント

設計・仕様変更時はPROJECT.mdを、PRマージ等の区切りと作業終了時はSESSION.mdを更新する。

## トークン方針

- 大きなファイルは必要範囲（offset/limit等）のみ読み、差分編集で修正する。一度読んだファイルは再読み込みしない
- 中間データはファイルに書き出し要約のみ確認する
- 独立ツール呼び出しは1メッセージにまとめ並行実行する
- PR状態確認は必要項目のみ取得し、本文全文を繰り返し取得しない。編集確認はツール成否で足りる
- ファイルサイズ規則はコード＝行数／ドキュメント＝字数で分ける。コードは目安1000行超で機能単位に分割
- ドキュメント目安上限：AGENTS.md 8,000字／CLAUDE.md 4,000字／PROJECT.md 12,000字／SESSION.md 6,000字。超過は内容を削るシグナル（1項目1行、120字超は削るか理由・経緯をPR/Issueへ移す）
- SESSION.md超過時は①完了タスク→②引継ぎ事項中の完了Issueメモ（クローズ済）の順で削る（正本はgit/PR）。現在作業中タスク・恒久的制約は削らない
- PROJECT.mdは決定事項のみ記載し、検討経緯・却下案・変更理由はPR/Issueに残す（追記でなく置き換えが基本）
- 超過時は機能単位でdocs/へ分割し、索引（各ファイル1行＋読むタイミング）を必ず追記する
