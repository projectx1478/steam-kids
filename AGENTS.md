# AGENTS.md

OpenCode（DeepSeek-V3 等）向けの行動規範。CLAUDE.md / PROJECT.md と重複する規約は再掲せず参照する。

## 起動時の必須手順
このセッションの最初のツール呼び出しは `git fetch origin main && git log --oneline HEAD..origin/main`（Codespaceは再開時も前回の作業ツリーが残り、古いファイルを最新と誤認するため。出力があれば `git merge origin/main` で取り込む）。次に `CLAUDE.md` と `PROJECT.md` の Read、次に `SESSION.md`。読む前に編集・提案を始めない。矛盾時は CLAUDE.md > PROJECT.md > チャット指示の順。Codespace起動直後で `main` ブランチにいる場合、編集を始める前に CLAUDE.md「GitHub運用」に従い `origin/main` 起点の作業ブランチへ切り替える。

## 調査フェーズ（編集前に必須）
変更対象を1文字も書く前に、(a) 対象ファイルを Read、(b) 変更する関数・変数の呼び出し元を grep、(c) 類似実装を1つ以上確認する。「たぶんこうなっている」で書かない。ファイルを読まずに import・関数名・API を書いてはならない。

## 計画フェーズ
変更するファイルと各ファイルで何を変えるかを箇条書きで提示してから着手する。3ファイル以上に及ぶ場合、または仕様が一意に定まらない場合はユーザーの承認を待つ。
設計・調査タスクはplanエージェントで開始する（Web UIでは入力欄左下のドロップダウンまたは`Ctrl/Cmd + .`で切り替え。Tabキー（`agent.cycle`）はTUI専用でWeb UIでは効かない。セレクタは`opencode.json`にカスタムエージェント定義がある場合のみ表示される）。buildに居続けると本節のガードは働かない。設計案を提示したら同一ターンで実装に進まず停止し、buildエージェントへの切り替えと、GUIのモデルピッカーでの実装用モデルの選び直しをユーザーに促す。

## 編集フェーズ（最小変更）
依頼された変更のみを行う。フォーマット統一・リネーム・未使用コード削除・コメント追加は、依頼に含まれる場合のみ。既存の命名規則とコーディングスタイルに合わせる。ファイル全文の write は新規作成時のみ。既存ファイルは必ず差分編集する。

## 検証フェーズ（指示がなくても必ず実行）
編集後、PROJECT.md「検証コマンド」表に列挙されたコマンドを実行する（CLAUDE.md「検証コマンド」節は実行義務のみを定め、コマンド一覧はPROJECT.mdにある）。すべて成功するまで報告しない。失敗した場合は原因を特定して修正し、再実行する。実行していないコマンドの結果を書いてはならない。テストを通すためにテスト自体を書き換える・skip する・期待値をコードに合わせることを禁止する（テストの誤りだと判断した場合は修正せず報告する）。

## 終了条件
「次に〜します」で終わらない。宣言した作業はそのターン内で実行する。停止してよいのは、(a) 全検証が通った、(b) ユーザーの判断が必要な分岐に到達した、(c) 3回試して直らない、のいずれか。PR作成は停止点（(b)相当）。マージはユーザーが行う、AIは`gh pr merge`等を実行しない。

## 禁止事項
CLAUDE.md「禁止事項」節を参照（重複記述しない）。

## 報告形式
前置き・手順の実況・コード全文再掲を書かない。「変更内容（ファイル単位1行）」「実行した検証コマンドと結果」「次のアクションまたは要確認事項」の3点のみ。日本語で回答する。

## トークン方針
1000行超・字数の目安上限（CLAUDE.md参照）超のファイルは必要範囲のみ offset/limit で読む。一度読んだファイルは再読み込みしない。独立した調査は1メッセージにまとめて並行実行する。
