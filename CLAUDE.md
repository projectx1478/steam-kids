# CLAUDE.md

## 優先順位
作業開始時の確認順序：
1. PROJECT.md（要件定義・設計・開発ルール）
2. SESSION.md（現在の開発状況・引き継ぎ情報）
3. 現在のチャットでの指示

チャット履歴は参考情報、仕様・設計・進捗はファイルが正。
必要なファイルのみ読み込む（全体走査は不用意に行わない）

## 開発方針
- 既存機能を壊さない差分修正が基本。不要なリファクタリング・範囲外の変更は行わない
- 既存の命名規則・コーディングスタイルを維持し、仕様不明時は推測せず確認する
- コード修正前に実装方針を提示し承認後に着手する

## トークン節約の方針
- 大きなファイルは必要範囲（offset/limit等）のみ読み、差分編集で修正する
- 中間データはファイルに書き出し要約のみ確認する
- 独立ツール呼び出しは1メッセージにまとめ並行実行する
- 一度読んだファイルは再読み込みしない
- PR状態確認は必要項目のみ取得し、本文全文を繰り返し取得しない
- 編集確認はツール成否で足りる
- ファイルサイズ規則はコード＝行数／ドキュメント＝字数で分ける
- コードは目安1000行超で機能単位に分割
- ドキュメント目安上限：CLAUDE.md 8,000字／PROJECT.md 12,000字／SESSION.md 6,000字。超過は内容を削るシグナル（1項目1行、120字超は削るか理由・経緯をPR/Issueへ移す）
- SESSION.md超過時は①完了タスク→②引継ぎ事項中の完了Issueメモ（クローズ済）の順で削る（正本はgit/PR）。現在作業中タスク・恒久的制約は削らない
- PROJECT.mdは決定事項のみ記載し、検討経緯・却下案・変更理由はPR/Issueに残す（追記でなく置き換えが基本）
- 超過時は機能単位でdocs/へ分割し、索引（各ファイル1行＋読むタイミング）を必ず追記する
- PRマージ・Issue起票の区切りや5時間/週間制限の強制停止時は/clearを使わず、SESSION.md更新後に**アーカイブ→新規セッション作成**を呼びかける
- 新規セッションはSESSION.mdと対応Issueのみ読み、チャット履歴は引き継がない

## モデル選択ルール
- 実装はSonnet以上を既定、Haikuは使わない（手戻りコストが節約分を上回るため）。原因調査・設計判断・conflict解消・PRマージ分岐はOpus/Sonnet（設計誤りは波及範囲大のため厚く配分）
- Haikuは確定済みテキストの非コード作業（複数ファイルへの同一コピー、typo・体裁修正）のみに限定。判断が1つでも入るなら使わない
- フェーズ転換時のモデル切替確認は不要（設計→実装のみ例外）
- Opus使用時は設計・調査・検証に専念しコード実装はしない。plan modeへ入り設計案提示でターンを終え、承認後も自分では実装せずIssueに記載しSonnetへ引き継ぐ（ユーザーが明示的にOpus実装を指示した場合のみ例外）

## バッチ実装
- 同時依頼の関連修正は1回の実装・1PRにまとめ（無関係な変更は混在させない）、コミットは論理単位で分けてよい
- PR作成・マージ確認のツール呼び出し回数削減を優先する

## 検証方針（AI代理実機確認）
- 設計案提示時は必ず「検証計画」を含め、確認項目はAI担当／ユーザー担当／省略（理由付き）に三分する
- ユーザー担当は人でなければ判定できない項目のみ（使い勝手、見た目、面白さ、体験の質）。動作・状態遷移・エラー処理・データ整合性・レイアウト崩れはAIが確認する
- AIはテストデータを自作し、外部APIは`page.route`で固定レスポンスに差替。手順は`.claude/verify/README.md`参照（検証時のみ）
- 判定はDOM（テキスト・属性・要素数・境界ボックス）で行う。スクリーンショットはユーザー確認用の保存物とし、AIが画像として読むのはDOM判定不能かつ1〜2枚で足りる場合のみ
- 省略基準：手動確認が1分以内の目視で済み、かつ自動化に大がかりな環境構築が必要な場合
- 完了時は既存シナリオも再実行し、「AI確認済み（結果）」「ユーザー確認依頼（手順・該当画像）」を報告

## GitHub運用
- mainへの直接pushは禁止。ブランチ作成→コミット→PR作成、Mergeは人
- 例外1：SESSION.md単独変更（`git diff --name-only origin/main...HEAD`で確認）は`git push origin <ブランチ>:main`で直接push可。直後に`<ブランチ>:<ブランチ>`もpushしHEADを揃える
- 例外2：`sync-template-broadcast.yml`のテンプレート同期PR（project-templateと完全一致・保護ルール無しの場合のみ）はActionsが自動マージ。AIは実行しない
- 配布対象`.github/sync-files.txt`／配布先`.github/sync-targets.json`（配布先で編集しない）
- 既存ブランチへのpush前にPRのマージ・クローズ有無を確認。マージ済みなら`rebuild-branch.sh <branch>`で再構築。push後はオープンPRが無ければ新規作成
- GitHub操作ツールは都度ToolSearchせず初回にまとめて取得
- ローカルmainは使わず信用しない。新規ブランチは`origin/main`起点、ローカルmain修正は`git fetch origin main && git branch -f main origin/main`でref上書き
- 作業再開・PRマージ後は`git fetch origin main && git log --oneline HEAD..origin/main`で確認し、未取り込みがあれば`git merge origin/main`後にファイルを読む
- OpenCode Web利用時の環境構築・起動手順は`.devcontainer/README.md`参照（利用時のみ）

## Issue運用
- バグ・改善点・仕様確認事項の発見時は直接修正せずIssue作成後に着手する（対応中タスクの軽微な修正は除く）
- PROJECT.mdは恒久的な仕様のみ記載し、個別タスクはIssueへ。対応Issueはクローズ参照する

## 機密情報のハードコーディング禁止
APIキー・DB認証情報・パスワード・トークン等の真の秘密情報は、クライアント側コード（Gitコミット対象）へ直書きせずサーバー側コンポーネントに隔離する。
クライアントは認証トークン経由でのみアクセスする。採用技術・例外の扱いはPROJECT.mdの技術設計に従う

## 回答方針
必要最小限の回答とし、コード全文・長い説明は要求時のみ行う。
実行報告は結果と次のアクションのみに絞り、手順実況・定型句は書かない。
判断を要する提案・トレードオフ・リスクの提示は削らない

## ドキュメント
設計・仕様変更時はPROJECT.mdを、PRマージ等の区切りと作業終了時はSESSION.mdを更新する

## 検証コマンド
コード変更後は指示なしでもPROJECT.md「検証コマンド」の全コマンドを実行する。
成功を確認してから報告する（未実行のコマンドの結果は書かない）。
テストを通すための書き換え・skip・期待値の合わせ込みは禁止

## 禁止事項
- 対象ファイル未読での編集／既存ファイルの全文write上書き
- 依頼範囲外の変更（自発的リファクタ・全体整形・未使用コード削除・コメント追加）
- 検証未実行の完了報告・結果記述
- `git reset --hard` / `git clean` / force push / `rm -rf`の無確認実行
- mainへの直接push、PRのマージ実行（`gh pr merge`・MCPの`merge_pull_request`等）。上記2例外を除きMergeは人
- 秘密情報（APIキー・トークン・パスワード）の直書き、.env等の内容をチャットへ貼ること
