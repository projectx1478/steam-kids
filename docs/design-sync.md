# 同期・バックエンド・機密情報管理

P3着手時に読む。P2までは外部通信を行わないため、本ドキュメントの内容は実装に影響しない。

## 同期方式

- **localStorage を正とする**。オフラインで学習を継続できること
- オンライン復帰時に未送信イベントをまとめて送信する
- 受信側は `eventId` で重複排除し、`ts` 昇順にマージする
- イベントは追記のみ。上書き・削除をしない

## 複数端末の紐付け（リンクコード方式）

- 既存端末で6文字コードを発行する（有効期限24時間、1回限り）
- 新端末でコードを入力すると同じ `learnerId` を共有する
- コードは英数字。紛らわしい文字（0/O、1/I/l）を除外する

## バックエンド選定（未決 / P3着手時までに決定）

| 案 | 内容 |
| --- | --- |
| A | Firebase（Firestore + 匿名認証） |
| B | GitHub Contents API + Cloudflare Workers（既存の他プロジェクト構成に揃える） |

決定時は本節を「決定事項」に書き換え、検討経緯はIssueに残す。

## 機密情報管理

採用技術が確定した時点で本節を確定させる。原則は以下。

- 課金・利用制限に直結するAPIキー、DB管理者権限の接続情報、認証パスワード・トークン・秘密鍵など、
  漏洩により金銭的損害や不正アクセスにつながる真の秘密情報は、クライアント側コード
  （Gitにコミットされるファイル）へ直書きせず、サーバー側コンポーネントに隔離する
- クライアントは認証トークン経由でのみアクセスする
- 公開前提の識別子（Firebase Web SDK の `firebaseConfig` 等）や公開エンドポイントURLは
  直書きを許容するが、値は1箇所へ集約し、変更・移行時は必ずユーザーに確認する
- 新たに「真の秘密情報」に該当する値を追加する場合は、直書きせず必ずユーザーに設計を確認する

## 同期してはいけないもの

PROJECT.md「プライバシー要件」を参照。氏名・学校名・学年、および呼び名（`label`）は
同期データに含めない。
